# pr_reviewer.py uses only the Python standard library (urllib, json, argparse, etc.)
# so there is nothing to install. This file exists so CI/setup steps have
# something explicit to point `pip install -r requirements.txt` at.
